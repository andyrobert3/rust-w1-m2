fn main() {
    let x = 7;

    // if statement written here as an expression on the right side of the = sign
    let is_less_than_5 = if x < 5 { true } else { false };

    println!("is_less_than_5: {}", is_less_than_5)
}
